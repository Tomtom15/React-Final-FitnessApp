import colors from '../../theme/colors';
import Helpers from '../../utils/Helper';

const styles = {
    containerStyle: {
        backgroundColor: colors.primaryBtn,
        height: Helpers.getNewDynamicSize(45),
        minHeight: Helpers.getNewDynamicSize(45),
        elevation: 2,
    }
};
export default styles